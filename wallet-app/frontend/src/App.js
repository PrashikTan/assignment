import React from 'react';
import ReactDOM from 'react-dom';
import WalletApp from './WalletApp';
import './index.css';

const root = document.getElementById('root');
ReactDOM.render(<WalletApp />, root);
